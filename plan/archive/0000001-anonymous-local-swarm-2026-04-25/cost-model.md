---
title: "Cost Model - Autonomous Local Swarm"
summary: "Compute, storage, and third-party cost estimates."
status: "active"
version: "0.1.0"
---
# Cost Model - Autonomous Local Swarm

**Skill:** spec-agent
**Feature:** 0000001-autonomous-local-swarm
**Version:** 0.1.0

---

## Summary

This is a local-first system. All inference runs on local hardware via Ollama. There are no cloud compute costs. Third-party API costs are minimal and bounded by free tiers.

---

## Compute

| Resource | Cost | Notes |
|---|---|---|
| Ollama inference | $0 | Local GPU; operator's existing hardware (8GB VRAM) |
| Orchestrator process | $0 | Local CPU; negligible vs inference workload |
| mcp-reddit Node process | $0 | Local; ~50MB RAM |

## Storage

| Resource | Estimated Size | Cost |
|---|---|---|
| swarm.db (SQLite) | < 100MB per project run | $0 — local disk |
| obs.db (DuckDB) | < 500MB per project run (columnar compression) | $0 — local disk |
| Context sidecars (JSON) | < 10MB per project run | $0 — local disk |
| Output artifacts | < 50MB per project run | $0 — local disk |

## Third-Party API Costs

| Service | Free Tier | Estimated Usage | Cost |
|---|---|---|---|
| Brave Search API | 2,000 queries/month free | ~100–500 queries per project run | $0 (within free tier for light usage) |
| GitHub API | 5,000 requests/hour (authenticated) | ~50–200 per run | $0 |
| Reddit API | 100 requests/minute (free app) | ~50–200 per run | $0 |
| Wikipedia, HN, ArXiv, YouTube Transcripts | Free / no key | Unlimited reasonable use | $0 |
| Memory MCP, context-mode, fetch | Free / local | N/A | $0 |

## Cost Boundary

No cloud spend. Total operating cost is electricity (GPU inference) + free API tiers. No budget constraint required.

---

*Generated by spec-agent.*
